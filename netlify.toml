[build]
  command = "npm install"
  publish = "public"
  functions = "netlify/functions"

# Optional: redirect clean paths to functions (not strictly needed if calling .netlify/functions/* directly)
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
